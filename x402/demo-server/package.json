{
  "name": "warda-demo-vendor",
  "private": true,
  "type": "module",
  "dependencies": {
    "@warda_protocol/kaspa": "^0.5.0",
    "@warda_protocol/vendor": "^0.2.0",
    "@warda_protocol/borsh": "^0.1.0",
    "kaspa-wasm32-sdk": "*"
  },
  "description": "A real HTTP 402 endpoint agents can be pointed at. One function; the covenant decides what may be paid, this only checks the money arrived.",
  "engines": {
    "node": "22.x"
  }
}

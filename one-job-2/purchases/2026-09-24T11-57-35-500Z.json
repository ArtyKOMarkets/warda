{
  "_comment": "Written by agent-002/tools/buy.ts. One file per attempt, refusals included: a purchase log that only records successes is a sales brochure.",
  "at": "2026-09-24T11:57:35.500Z",
  "agent": "ONE-JOB-VERIFY",
  "url": "https://warda-node.tailc0c0ec.ts.net:8443/v1/report",
  "task": "audit warda_grant.sil",
  "proof": {
    "header": "",
    "txid": "e92bd705f312383036fefecc9dce69b55d16bf4f55c4e1656e39ee5f9b9afebe",
    "amountSompi": "4000000",
    "payTo": "kaspatest:qz89as2n6h3lpxdkfkgdsej2ed3px0vxl3jwdy7hxv9rm39t7ynjgq4ala62q"
  },
  "outcome": "bought",
  "status": 200,
  "quoted": null,
  "txid": "e92bd705f312383036fefecc9dce69b55d16bf4f55c4e1656e39ee5f9b9afebe",
  "sellerClaimed": null,
  "resumedFrom": "e92bd705f312383036fefecc9dce69b55d16bf4f55c4e1656e39ee5f9b9afebe",
  "response": {
    "html": "<!doctype html>\n<html lang=\"en\"><head><meta charset=\"utf-8\"><title>Covenant analysis</title>\n<style>\n  :root{--bg:#08090b;--surface:#0e1013;--line:#1f242b;--ink:#eceef1;--mid:#a4abb4;--dim:#6b737d;--accent:#14d7c1}\n  html{-webkit-print-color-adjust:exact;print-color-adjust:exact}\n  body{margin:0;background:var(--bg);color:var(--ink);font:15px/1.6 -apple-system,BlinkMacSystemFont,\"Segoe UI\",Helvetica,Arial,sans-serif}\n  .page{max-width:56rem;margin:0 auto;padding:52px 40px 72px}\n  h1{font-size:32px;letter-spacing:-.03em;margin:0 0 8px}\n  .sub{color:var(--dim);font-size:13px;margin:0 0 22px}\n  .note{border:1px solid var(--line);border-left:2px solid var(--accent);background:var(--surface);\n    padding:15px 17px;border-radius:0 10px 10px 0;color:var(--mid);font-size:14px;max-width:44rem}\n  pre{background:var(--surface);border:1px solid var(--line);border-radius:12px;padding:20px 22px;\n    overflow-x:auto;font:12.5px/1.65 ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;color:var(--mid);\n    white-space:pre-wrap;word-break:break-word}\n  footer{margin-top:36px;padding-top:14px;border-top:1px solid var(--line);color:var(--dim);font-size:12px}\n  @page{size:A4;margin:14mm 12mm}\n</style></head><body><div class=\"page\">\n<p class=\"sub\">2026-09-24 11:57 UTC</p>\n<h1>Covenant analysis</h1>\n<div class=\"note\"><b>This is not a statement that the covenant is secure.</b> It reports what is\ntrue of the compiled artefact and what conditions the source refuses on. Whether those\nconditions are the right ones, and whether each is where its author thinks it is, needs a\ntransaction the engine will accept — and that needs a builder written for this covenant's\nentrypoints.</div>\n<pre>WardaGrant  ·  /var/folders/mr/scf_xb5d51nfl56pv_zpz87c0000gn/T/scan-UxLDmI/covenant.sil\n────────────────────────────────────────────────────────────\n\nCONSTRUCTOR  21 arguments\n  principalKey             pubkey\n  revocationKey            pubkey\n  maxFee                   int\n  genesisAgentKey          byte[32]\n  genesisBudgetTotal       int\n  genesisMaxPerSpend       int\n  genesisEpochLimit        int\n  genesisEpochLength       int\n  genesisRecipientsRoot    byte[32]\n  genesisNotBefore         int\n  genesisExpiresAt         int\n  genesisDelegationDepth   int\n  genesisTemplateId        byte[32]\n  templatePrefixLen        int\n  templateSuffixLen        int\n  maxProofDepth            int\n  initSpentTotal           int\n  initReserved             int\n  initEpochIndex           int\n  initEpochSpent           int\n  initReserveRoot          byte[32]\n\nENTRYPOINTS  6\n  covenant   spend(State prevState, State newState, int amount, byte[32] recipient, byte[32][] proofSiblings, bool[] proofSiblingIsLeft, int claimedDaa, sig agentSig)\n             #[covenant.singleton]\n  covenant   delegate(State prevState, State[] newStates, byte[32][] subsetSiblings, bool[] subsetSiblingIsLeft, sig agentSig)\n             #[covenant.fanout]\n  covenant   reabsorb(State prevState, State[] newStates, int childIdx, byte[32] prevRoot, sig agentSig)\n             #[covenant.fanout]\n  entry      settle(sig s)\n  entry      revoke(sig s)\n  entry      reclaim(sig s)\n\nWHAT IT REFUSES ON\n\n  spend — 34 conditions\n    require(newState.agentKey == prevState.agentKey);\n    require(newState.budgetTotal == prevState.budgetTotal);\n    require(newState.maxPerSpend == prevState.maxPerSpend);\n    require(newState.epochLimit == prevState.epochLimit);\n    require(newState.epochLength == prevState.epochLength);\n    require(newState.recipientsRoot == recipientsRoot);\n    require(newState.notBefore == prevState.notBefore);\n    require(newState.expiresAt == prevState.expiresAt);\n    require(newState.delegationDepth == delegationDepth);\n    require(newState.templateId == prevState.templateId);\n    require(newState.reserveRoot == prevState.reserveRoot);\n    require(amount &gt; 0);\n    require(amount &lt;= maxPerSpend);\n    require(claimedDaa &gt;= notBefore);\n    require(tx.daa &gt;= claimedDaa);\n    require(committed &lt;= budgetTotal);\n    require(amount &lt;= budgetTotal - committed);\n    require(epochLength &gt; 0);\n    require(currentEpoch &gt;= prevState.epochIndex);\n    require(claimedDaa &lt; expiresAt);\n    require(spentThisEpoch &lt;= epochLimit);\n    require(amount &lt;= epochLimit - spentThisEpoch);\n    require(merkleRoot(recipient, proofSiblings, proofSiblingIsLeft) == recipientsRoot);\n    require(newState.spentTotal == prevState.spentTotal + amount);\n    require(newState.reserved == prevState.reserved);\n    require(newState.epochIndex == currentEpoch);\n    require(newState.epochSpent == spentThisEpoch + amount);\n    require(OpAuthOutputCount(this.activeInputIndex) == 1);\n    require(OpAuthOutputIdx(this.activeInputIndex, 0) == 0);\n    require(tx.outputs[1].scriptPubKey == byte[](payTo));\n    require(tx.outputs[1].value == amount);\n    require(inValue &gt;= amount);\n    require(tx.outputs[0].value &gt;= inValue - amount - maxFee);\n    require(checkSig(agentSig, pubkey(agentKey)));\n\n  delegate — 38 conditions\n    require(child.budgetTotal &gt; 0);\n    require(committed &lt;= budgetTotal);\n    require(child.budgetTotal &lt;= budgetTotal - committed);\n    require(child.maxPerSpend &lt;= maxPerSpend);\n    require(child.epochLimit &lt;= epochLimit);\n    require(child.epochLength == epochLength);\n    require(child.notBefore &gt;= notBefore);\n    require(child.expiresAt &lt;= expiresAt);\n    require(child.delegationDepth &lt; delegationDepth);\n    require(foldFromNode(child.recipientsRoot, subsetSiblings, subsetSiblingIsLeft) == recipients…\n    require(child.spentTotal == 0);\n    require(child.reserved == 0);\n    require(child.epochIndex == 0);\n    require(child.epochSpent == 0);\n    require(child.templateId == templateId);\n    require(child.reserveRoot == emptyStack);\n    require(parentNext.agentKey == agentKey);\n    require(parentNext.budgetTotal == budgetTotal);\n    require(parentNext.maxPerSpend == maxPerSpend);\n    require(parentNext.epochLimit == epochLimit);\n    require(parentNext.epochLength == epochLength);\n    require(parentNext.recipientsRoot == recipientsRoot);\n    require(parentNext.notBefore == notBefore);\n    require(parentNext.expiresAt == expiresAt);\n    require(parentNext.delegationDepth == delegationDepth);\n    require(parentNext.spentTotal == spentTotal);\n    require(parentNext.epochIndex == epochIndex);\n    require(parentNext.epochSpent == epochSpent);\n    require(parentNext.reserved == reserved + child.budgetTotal);\n    require(parentNext.templateId == templateId);\n    require(parentNext.reserveRoot == pushed);\n    require(OpAuthOutputCount(this.activeInputIndex) == 2);\n    require(OpAuthOutputIdx(this.activeInputIndex, 0) == 0);\n    require(OpAuthOutputIdx(this.activeInputIndex, 1) == 1);\n    require(inValue &gt;= child.budgetTotal);\n    require(tx.outputs[1].value == child.budgetTotal);\n    require(tx.outputs[0].value &gt;= inValue - child.budgetTotal - maxFee);\n    require(checkSig(agentSig, pubkey(agentKey)));\n\n  reabsorb — 28 conditions\n    require(childIdx &gt;= 0);\n    require(childIdx &lt; tx.inputs.length);\n    require(childIdx != this.activeInputIndex);\n    require(reserveRoot == pushed);\n    require(newState.reserveRoot == prevRoot);\n    require(child.spentTotal &gt;= 0);\n    require(child.budgetTotal &gt; 0);\n    require(child.reserved == 0);\n    require(reserved &gt;= child.budgetTotal);\n    require(newState.reserved == reserved - child.budgetTotal);\n    require(newState.spentTotal == spentTotal + child.spentTotal);\n    require(newState.spentTotal + newState.reserved &lt;= budgetTotal);\n    require(newState.agentKey == agentKey);\n    require(newState.budgetTotal == budgetTotal);\n    require(newState.maxPerSpend == maxPerSpend);\n    require(newState.epochLimit == epochLimit);\n    require(newState.epochLength == epochLength);\n    require(newState.recipientsRoot == recipientsRoot);\n    require(newState.notBefore == notBefore);\n    require(newState.expiresAt == expiresAt);\n    require(newState.delegationDepth == delegationDepth);\n    require(newState.templateId == templateId);\n    require(newState.epochIndex == epochIndex);\n    require(newState.epochSpent == epochSpent);\n    require(OpAuthOutputCount(this.activeInputIndex) == 1);\n    require(OpAuthOutputIdx(this.activeInputIndex, 0) == 0);\n    require(tx.outputs[0].value &gt;= combined - maxFee);\n    require(checkSig(agentSig, pubkey(agentKey)));\n\n  settle — 6 conditions\n    require(checkSig(s, revocationKey));\n    require(tx.inputs.length == 2);\n    require(parent.budgetTotal &gt; 0);\n    require(OpAuthOutputCount(parentIdx) == 1);\n    require(OpAuthOutputIdx(parentIdx, 0) == 0);\n    require(tx.outputs[0].value &gt;= combined - maxFee);\n\n  revoke — 3 conditions\n    require(checkSig(s, revocationKey));\n    require(tx.outputs[0].scriptPubKey == byte[](toPrincipal));\n    require(tx.outputs[0].value &gt;= tx.inputs[this.activeInputIndex].value - maxFee);\n\n  reclaim — 4 conditions\n    require(tx.daa &gt;= expiresAt);\n    require(checkSig(s, principalKey));\n    require(tx.outputs[0].scriptPubKey == byte[](toPrincipal));\n    require(tx.outputs[0].value &gt;= tx.inputs[this.activeInputIndex].value - maxFee);\n\n  113 conditions in total.\n\nCOMPILED\n  bytecode           6738 bytes, with every integer argument at 4\n  script-size limit  1000000 — 0.674% of it at these arguments\n  abi                6 functions\n                     __covenant_entrypoint_auth_spend\n                     __covenant_entrypoint_auth_delegate\n                     __covenant_entrypoint_auth_reabsorb\n                     settle\n                     revoke\n                     reclaim\n  state region       231 bytes at offset 1\n\n  SIZE DEPENDS ON\n    maxProofDepth            4 → 8: +568 bytes\n    A size or headroom figure quoted without its constructor is not a\n    figure about this covenant. Ask what that argument was set to.\n\n────────────────────────────────────────────────────────────\n113 conditions is the size of this covenant's refusal surface. If the\ndocumentation somebody hands you with it names fewer, the difference is what\nnobody has written down — not necessarily a defect, and not necessarily not.\n\nWhat this pass CANNOT tell you: whether those 113 conditions are the right\nones, and whether each is where its author thinks it is. Both need a\ntransaction the engine will accept, and that needs a builder for this\ncovenant's entrypoints — see PORTING.md. Compute budget and peak stack are\nin the same position: they are measured from an execution, not from source.\n</pre>\n<footer>Produced by the Warda covenant auditor. The analysis is reproducible from the same\nsource with <code>cargo run --bin scan</code>; this document adds a date and a shape you can send.</footer>\n</div></body></html>",
    "paidTo": "kaspatest:qz89as2n6h3lpxdkfkgdsej2ed3px0vxl3jwdy7hxv9rm39t7ynjgq4ala62q",
    "settledBy": "e92bd705f312383036fefecc9dce69b55d16bf4f55c4e1656e39ee5f9b9afebe",
    "verified": "a UTXO at this endpoint's payee address, from that transaction, for exactly the quoted amount",
    "readFrom": "this vendor's own node"
  },
  "resolvedBy": {
    "at": "2026-09-24T11:57:36.483Z",
    "status": 200,
    "record": "2026-09-24T11-57-35-500Z.json"
  }
}

# Copy to ops/auditor.env, which is gitignored, and fill in.
#
# QUOTE_SECRET is a password. Generate it once and keep it:
#
#     openssl rand -hex 32
#
# It must not change between restarts — see run-auditor.sh for why.

PAY_TO=kaspatest:qz89as2n6h3lpxdkfkgdsej2ed3px0vxl3jwdy7hxv9rm39t7ynjgq4ala62q
QUOTE_SECRET=
PRICE_SOMPI=4000000
PORT=8787
WARDA_NETWORK=testnet-10

# Optional: your own node, rather than one found through a public resolver.
# WARDA_RPC_JSON=ws://127.0.0.1:18210
